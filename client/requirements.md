## Packages
framer-motion | Smooth page transitions and hover animations
lucide-react | Iconography
clsx | Class name utility (already likely installed but good to explicit)
tailwind-merge | Class name merging (already likely installed)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
API provided via @shared/routes
Images are external URLs
Dark mode is default and mandatory
